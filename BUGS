Not a complete list of bugs

Outstanding:
- possible to download a zip file and have unzip get stuck due to replacing previous downloaded file
  unzip force command line option?

Fixed:

- Manage dialog will quit on cancel unconditionally
- Plugin name is sometimes empty - now shows installed name if product name is empty